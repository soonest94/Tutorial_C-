#include <stdio.h>

int main(void)
{
  int x=100;

  printf("100 is %d as decimal counting system. \n", x);
  printf("100 is %o as octal counting system. \n", x);
  printf("100 is %x as hexadecimal counting system. \n", x);
  return 0;
}
